name: Build Tweak

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        brew install ldid

    - name: Setup Theos
      run: |
        git clone --recursive https://github.com/theos/theos.git $HOME/theos
        mkdir -p $HOME/theos/sdks
        SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)
        ln -s "$SDK_PATH" $HOME/theos/sdks/iPhoneOS.sdk
        echo "THEOS=$HOME/theos" >> $GITHUB_ENV

    - name: Build
      run: |
        export THEOS=$HOME/theos
        make clean
        make
        echo "=== Finding dylib ==="
        find . -name "*.dylib" -type f

    - name: Sign dylib
      run: |
        DYLIB=$(find . -name "BaiduPanTroll*.dylib" -type f | head -1)
        echo "Found dylib: $DYLIB"
        if [ -n "$DYLIB" ]; then
          ldid -S "$DYLIB"
          echo "Signed with ldid -S"
          cp "$DYLIB" ./BaiduPanTroll.dylib
        fi

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: BaiduPanTroll.dylib
        path: BaiduPanTroll.dylib
        if-no-files-found: warn
